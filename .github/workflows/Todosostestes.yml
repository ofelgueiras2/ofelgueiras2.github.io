name: RSelenium Firefox Selenium Server (remoteDriver)

on:
  workflow_dispatch:

jobs:
  selenium-test-remotedriver:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Instalar dependências do sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb openjdk-11-jre-headless libcurl4-openssl-dev libssl-dev libxml2-dev wget libdbus-glib-1-2 net-tools

      - name: Instalar Firefox 118.0
        run: |
          wget https://ftp.mozilla.org/pub/firefox/releases/118.0/linux-x86_64/en-US/firefox-118.0.tar.bz2
          sudo tar xjf firefox-118.0.tar.bz2 -C /opt
          sudo ln -sf /opt/firefox/firefox /usr/bin/firefox
          firefox --version

      - name: Instalar Geckodriver v0.36.0
        run: |
          wget https://github.com/mozilla/geckodriver/releases/download/v0.36.0/geckodriver-v0.36.0-linux64.tar.gz
          sudo tar xzf geckodriver-v0.36.0-linux64.tar.gz -C /usr/local/bin
          sudo chmod +x /usr/local/bin/geckodriver
          geckodriver --version

      - name: Baixar Selenium Server 4.29.0
        run: |
          wget https://github.com/SeleniumHQ/selenium/releases/download/selenium-4.29.0/selenium-server-4.29.0.jar -O selenium-server.jar

      - name: Iniciar Xvfb e Selenium Server (robusto)
        run: |
          sudo pkill Xvfb || true
          sudo rm -rf /tmp/.X99-lock /tmp/.X11-unix/X99
          Xvfb :99 -screen 0 1280x1024x24 &
          sleep 5
          echo "DISPLAY=:99" >> $GITHUB_ENV

          sudo pkill -f selenium-server || true
          nohup java \
            -Dwebdriver.gecko.driver=/usr/local/bin/geckodriver \
            -Dwebdriver.firefox.bin=/usr/bin/firefox \
            -jar selenium-server.jar standalone \
            --host 127.0.0.1 --port 4444 \
            --selenium-manager false \
            > selenium.log 2>&1 &

          sleep 10
          netstat -tuln | grep '127.0.0.1:4444' || (cat selenium.log && exit 1)

      - name: Setup R environment
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.4.3'

      - name: Instalar RSelenium
        run: |
          Rscript -e 'install.packages("RSelenium", repos="https://cloud.r-project.org")'

      - name: Teste RSelenium (remoteDriver)
        env:
          DISPLAY: :99
        run: |
          Rscript -e '
            library(RSelenium)
            remDr <- remoteDriver(
              remoteServerAddr = "127.0.0.1",
              port = 4444L,
              browserName = "firefox",
              extraCapabilities = list(
                "moz:firefoxOptions" = list(args = list("--headless"))
              )
            )

            remDr$open(silent = TRUE)
            remDr$navigate("https://www.google.com")
            Sys.sleep(5)

            current_url <- remDr$getCurrentUrl()[[1]]
            page_title <- remDr$getTitle()[[1]]
            cat("URL atual:", current_url, "\n")
            cat("Título:", page_title, "\n")

            remDr$close()
          '

      - name: Mostrar log Selenium Server (sempre)
        if: always()
        run: cat selenium.log
